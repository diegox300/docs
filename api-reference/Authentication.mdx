# Autenticação

Todas as requisições feitas à API de atualização de veículos devem ser autenticadas. A API utiliza o esquema de autenticação **Bearer Token**.

Você deve incluir um token secreto no cabeçalho `Authorization` de cada requisição.

```md
Authorization: Bearer <SEU_TOKEN_SECRETO>
```